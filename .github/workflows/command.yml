on:
  workflow_dispatch:

jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - run: |
          deprecated=true
          if [["$deprecated" = "true"]]; then
            echo "::warning::Deprecated function found"
          fi
